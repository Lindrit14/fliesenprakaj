version: "3.9"

services:
  fliesenprakaj:
    build: .
    container_name: fliesenprakaj
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
    restart: unless-stopped
